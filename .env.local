# Cumulus3 Local Development Configuration

# Database
# Path to SQLite database file (relative or absolute path)
DB_PATH=./data/database/cumulus3.db

# Storage
# Directory for storing volume files (relative or absolute path)
DATA_DIR=./data/volume1 

# Maximum size of a single volume file before creating a new one
# Format: number + unit (KB, MB, GB, TB)
DATA_FILE_SIZE=100MB

# Maximum allowed size for uploaded files
# Format: number + unit (KB, MB, GB)
MAX_UPLOAD_FILE_SIZE=50MB

# Network configuration
# Port on which the server listens (default: 8800)
SERVER_PORT=8800

# Address to bind the server to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
SERVER_ADDRESS=0.0.0.0

# Compression
# File compression mode: None, Auto, gzip, zstd
# - None: No compression
# - Auto: Automatic compression using zstd (only if size reduction >= MINIMAL_COMPRESSION)
# - gzip: Always compress with gzip
# - zstd: Always compress with zstd (faster and better compression than gzip)
USE_COMPRESS=Auto

# Minimum compression ratio threshold for Auto mode (percentage only)
# File will be stored compressed only if size reduction >= this value
# Format: number% (e.g., 10% means file must be at least 10% smaller when compressed)
MINIMAL_COMPRESSION=10%

# Cleanup interval for expired files
# Format: number + unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 30s, 5m, 1h, 7d
CLEANUP_INTERVAL=1h

# Logging
# Log level: DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO

# Log format: text | json (json recommended for production)
LOG_FORMAT=text

# Enable colored log output: true | false
LOG_COLOR=true